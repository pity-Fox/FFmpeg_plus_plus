# ============================================================
# FFmpeg++ Dependencies
# ============================================================
# 阶段1 (backend):  纯 Python 标准库，无需额外 pip 依赖
# 阶段2 (简易 GUI):  PySide6
# 构建:             Nuitka (编译为单个 exe)
# ============================================================

# ── 阶段2/3 GUI ──
PySide6>=6.6.0

# ── 构建工具 (仅开发时需要) ──
nuitka>=2.0

# ── Nuitka 推荐搭配 (可选，提升兼容性) ──
# ordered-set>=4.1     # 稳定导入顺序
# zstandard>=0.22      # 压缩加速
